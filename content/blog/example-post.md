---
title: "Draft: Building government software while freelancing on the side"
description: "A starting outline for your first real post — replace this with your own story before publishing."
date: "2026-08-12"
tags: ["government", "freelance", "career"]
published: false
---

This file is a **template**, not a real post — that's why `published: false` is set in
the frontmatter above. Posts with `published: false` never show up on `/blog` or at
their own URL, so it's safe to leave this in the repo as a working example.

## How this works

- Add a new file to `content/blog/your-post-slug.md`. The filename becomes the URL:
  `/blog/your-post-slug`.
- Fill in the frontmatter block at the top (`title`, `description`, `date`, `tags`,
  and `published`). Set `published: true` when it's ready to go live.
- Write the body below the frontmatter in plain Markdown — headings, lists, links,
  bold/italic, code blocks, blockquotes, and tables all work (via GitHub-flavored
  Markdown), and code blocks get syntax highlighting automatically.
- Commit and push. There's no database or admin login — the file *is* the post.

## A starting outline for the post you described

You mentioned wanting to write about being a senior government software developer
while also freelancing on the side, and what you've learned doing both. Rather than
guessing at the specifics of your own experience, here's a structure you can fill in
with the real details — the same "concrete over generic" principle that shaped the
rest of this site's content applies here too:

1. **The setup** — what the government role actually involves day to day (systems,
   scale, constraints like compliance/procurement/security review that don't exist in
   most freelance work).
2. **Why you also freelance** — the real reason, not a generic "diversify income"
   line. What does freelance work give you that the government role doesn't (variety,
   different stacks, pace, client exposure)?
3. **What actually changed how you work** — one or two specific lessons that
   transferred *between* the two contexts. E.g. something a compliance-heavy
   government project taught you about scoping freelance work, or something a
   freelance client relationship taught you about stakeholder communication back at
   the office.
4. **The friction** — time management, context-switching, what you'd tell someone
   considering the same setup. Honesty here is what makes this worth reading over a
   generic "how to balance two jobs" listicle.
5. **One concrete example** — a real project, decision, or moment from either side
   that illustrates the point better than a paragraph of advice would.

```ts
// code blocks render with syntax highlighting, in case a post needs one
export function example() {
  return "delete this file's content and write the real thing";
}
```

> Delete everything above this line before publishing, or start a fresh file — this
> one is meant to be a reference, not your first live post.
